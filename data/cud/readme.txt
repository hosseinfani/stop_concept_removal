cud: concepts by a user at a day
This folder contains documents.

We assume that each user at "each time interval" (daily/hourly) is representing one single documents as follows:
a document = all concepts that are used in all tweets the user at i-th time interval

The document filename is the userid and the index of time interval (day# or hour#).